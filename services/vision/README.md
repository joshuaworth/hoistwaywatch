# Vision service

Responsibilities:
- Motion detection primitives (frame differencing / background subtraction)
- Zone-based detection outputs
- Optional: local-only person detector (must degrade gracefully)

The MVP should work without ML: visibility + motion-in-zone alerts.

