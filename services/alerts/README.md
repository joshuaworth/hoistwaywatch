# Alerts service

Responsibilities:
- Drive local outputs: siren, speaker, strobe, GPIO relays (when available)
- Local notifications (LAN), if configured
- Durable event log (privacy-sensitive; recordings are optional)

Alerts should always include a clear explanation of what triggered them.

