# Rules service

Responsibilities:
- Deterministically evaluate incoming events into alerts
- Provide **explanations** (rule ID, inputs, thresholds, zone IDs)
- Keep rules configurable (e.g., YAML/JSON), auditable, and versioned

Example rules:
- motion_in_zone
- person_in_zone
- motion_and_person
- camera_offline

