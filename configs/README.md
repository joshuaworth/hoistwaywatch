# Configs

This folder contains example configuration files for local deployments.

- `example-site.yaml`: site + camera + alert defaults (privacy-first)
- `example-zones.json`: example normalized polygons (0–1) per camera

