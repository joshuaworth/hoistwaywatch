site:
  name: "Example jobsite"
  timezone: "UTC"

cameras:
  - id: "cam1"
    name: "Hoistway main"
    # One of:
    # - type: uvc (USB webcam)
    # - type: rtsp (IP camera)
    type: "rtsp"
    rtsp_url: "rtsp://user:pass@192.168.1.50:554/stream1"

alerts:
  local_audio: true
  local_strobe: false
  lan_notifications: false

storage:
  # privacy-first default: no recordings
  recording_enabled: false
  retention_days: 0

