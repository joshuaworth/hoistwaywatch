name: CI

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  repo-hygiene:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run scaffold checks
        run: ./scripts/ci/ci.sh

